name: CI

on:
  push:
    tags:
      - '*'

permissions:
  contents: write

jobs:
  lint-and-test:
    name: Lint and test
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Test
        run: npm test -- --runInBand

  build:
    name: Build
    needs: lint-and-test
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            folder: "everbot-linux"
            target: node18-linux-x64
            executable: everbot
            archive: everbot-linux-x64.zip
          - os: macos-13
            folder: "everbot-macos"
            target: node18-macos-x64
            executable: everbot
            archive: everbot-macos-x64.zip
          - os: windows-latest
            folder: "everbot-windows"
            target: node18-win-x64
            executable: everbot.exe
            archive: everbot-win-x64.zip
    runs-on: ${{ matrix.os }}

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18.20.8
          cache: npm

      # Fix for node-gyp on Windows runner with Node 18
      - name: Fix node-gyp for Visual Studio on Windows
        if: matrix.os == 'windows-latest'
        run: |
          npm install -g node-gyp@latest
          npm config set node_gyp (Get-Command node-gyp).Path

      - name: Install dependencies
        run: npm ci

      - name: Build JS bundle
        run: npm run build

      - name: Install pkg
        run: npm install --global pkg@5.8.1

      - name: Package executable
        shell: bash
        run: |
          mkdir -p "artifacts/${{ matrix.folder }}"
          
          # Copy static asset dependencies
          cp config.yaml "artifacts/${{ matrix.folder }}/config.yaml"
          
          # Copy native SQLite bindings compiled for this specific host OS
          cp node_modules/better-sqlite3/build/Release/better_sqlite3.node "artifacts/${{ matrix.folder }}/better_sqlite3.node"
          
          # Package JavaScript with pkg
          pkg dist/cli/bundle.js --target "${{ matrix.target }}" --output "artifacts/${{ matrix.folder }}/${{ matrix.executable }}"
          chmod +x "artifacts/${{ matrix.folder }}/${{ matrix.executable }}" 2>/dev/null || true

      - name: Create Unix zip
        if: matrix.os != 'windows-latest'
        shell: bash
        run: |
          cd "artifacts/${{ matrix.folder }}"
          zip -r "../../${{ matrix.archive }}" .

      - name: Create Windows zip
        if: matrix.os == 'windows-latest'
        shell: pwsh
        run: |
          Compress-Archive -Path "artifacts/${{ matrix.folder }}/*" -DestinationPath "${{ matrix.archive }}"

      - name: Upload platform zip artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.archive }}
          path: ${{ matrix.archive }}
          if-no-files-found: error

  release:
    name: Publish release
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Download platform artifacts
        uses: actions/download-artifact@v4
        with:
          path: release-assets
          pattern: everbot-*.zip
          merge-multiple: true

      - name: Publish release assets
        uses: softprops/action-gh-release@v2
        with:
          files: release-assets/*.zip
          generate_release_notes: true
